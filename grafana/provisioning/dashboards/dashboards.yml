apiVersion: 1

providers:
  - name: expense-tracker-v1-2-traces-logs
    orgId: 1
    folder: 'Expense Tracker — v1.2 Traces & Logs'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
